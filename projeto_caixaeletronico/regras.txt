BANCO DE DADOS
database projeto_caixaeletronico;
formato do banco inno_DB;

tabela contas{
id int(11) primary key auto_increment;
titula varchat(100) utf8_general_ci;
agencia int(11);
conta int(11);
senha varchar(32);
saldo float;
}

tabela historico {
id int(11) primary key auto_increment;
id_conta int(11);
tipo tinyint(1);
valor float;
data_operacao datetime;
}

<-----------------REGRAS DO SISTEMA------------------------>
utilizar bootstrap,jquery mask,jquery validate, sweet alerts,javascript puro "talvez",CSS.
Começar pelo back-end.

LOGIN{
-não permitir o formulario ser enviado com os campos vazios, utilizar jquery validate (check)

-campo agencia apenas numeros e apenas 4 digitos e mostrar erro quando tiver com o campo menor que 4 e quando não existir essa agencia.Obs:utilizar jquery validate com jquery mask.(check)

-campo conta apenas numeros e apenas 5 digitos e mostrar erro quando tiver com o campo menor que 5 e quando não existir essa conta.Obs:utilizar jquery validate com jquery mask.(check)

-campo senha aceitar maximo 15 caracteres e minimo 3,letras maiusculas e minuscula e numeros apenas , não aceitar caracteres especiais, mostrar quando a tecla de capslock estiver ativada.(CHECK)

-mensagem de erro quando não houver conta ,ou nao haver agencia ou senha errada Obs:Utilizar session com bootstrap (CHECK)
}

INDEX{
-fazer uma menu vertical com os dados da conta ,talvez usar includes para repetir essa informção em varias abas.

-usar bootstrap para fazer a tabela de movimentação responsiva.
 
}

TRANSAÇÃO{
-editar o option de "Tipo de Conta" usar CSS;
-não deixar campo valor vazio utilizar require no HTML ,NÂO deixar fazer uma transação com valor 0,utilizar o jquery mask para o campo valor,
não deixar fazer uma retirada maior que o saldo disponível,talvez enviar um alert utilizando o sweet alerts
}